#include <set>
#include <vector>
#include <unordered_map>
#include <string>
#include <utility>
#include <cstddef>
using namespace std;

set<pair<int, int>> lsh (const vector<vector<int>>& minHashes, int numBands);